<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.logis_app.Mapper.LoginMapper">
    <select id="login" resultType="java.lang.String">
        SELECT password
        FROM user
        WHERE username = #{username}
    </select>

    <insert id="register" useGeneratedKeys="true" keyProperty="userId">
        INSERT INTO user (name, user_phone, username, password, email)
        VALUES (#{name}, #{userPhone}, #{username}, #{password}, #{email})
    </insert>


</mapper>